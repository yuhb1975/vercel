---
---

Document the `--non-interactive` option for `vercel blob` in the Vercel CLI storage skill reference: how it suppresses prompts across all blob commands and turns missing input (`missing_arguments`) and required consent (`confirmation_required`, needing `--yes`) into fail-fast structured errors.
